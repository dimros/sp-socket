{
    "name": "sp-socket",
    "version": "0.0.4",
    "description": "This program communicates with the serialport using the socket.io.",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "git://github.com/dimros/sp-socket.git"
    },
    "author": "dimros",
    "license": "MIT",
    "dependencies": {
        "socket.io":">=0.9.0",
        "serialport":">=1.1.1"
    },
    "engines":{
        "node": ">=0.8.0"
    }
}
